import './Pieces.css'

const Pieces = () => {
const position = new Array(8).fill('').map(x => new Array(8).fill(''))
console.log(position)
return <div className='pieces'></div>
}

export default Pieces
